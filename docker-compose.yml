version: '3'
services:
  app:
    container_name: financas
    image: financas:desenv
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./src:/var/www
